# INPI Heatmap Visual MVP - Environment Configuration
# Copy this file to .env and adjust values as needed

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# DINOv3 Model Configuration
# Options: small, base, large, giant
DINOV3_MODEL_SIZE=large

# Heatmap Configuration
# Threshold: pixels below this value become transparent (0-1)
HEATMAP_THRESHOLD=0.5
# Alpha: default overlay opacity (0-1)
HEATMAP_ALPHA=0.6

# Search Configuration
TOP_K=5
TOP_K_HEATMAPS=3

# Paths (optional - defaults are relative to app)
# IMAGES_DIR=/app/static/images
# EMBEDDINGS_CACHE_DIR=/app/embeddings_cache
# OUTPUTS_DIR=/app/outputs
